#include <stdio.h>
#include <locale.h>

int main(){

	setlocale (LC_ALL, "portuguese");

	/*Exercício 8 lista repetção:
	//Algorítmo para mostrar a soma e média de 3 notas para 10 alunos.
	int i;
	float n1, n2, n3, soma, media;
	char nome[50];
	for (i=0; i<10; i++){
		printf("Digite o nome do(a) aluno(a): ");
		scanf(" %s", &nome);
		printf("Digite a primeira nota do(a) aluno(a) %s: ", nome);
		scanf("%f", &n1);
		printf("Agora digite a segunda nota: ");
		scanf("%f", &n2);
		printf("Por fim, digite a terceira nota: ");
		scanf("%f", &n3);
		soma = n1+n2+n3;
		media = soma/3;
		printf("\nA soma das notas do(a) %s é: %.2f", nome, soma);
		printf("\nA média das notas do(a) %s é: %.2f\n\n", nome, media);
	}*/
	
	//Exercício 9 lista repetção:
	//Algorítmo que leia um número e diga se é primo ou não:
	int num;
	

return 0;
}

